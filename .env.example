# Copy to .env (local only). On Netlify, set these in Site settings → Environment
# Optional: increases GitHub API rate limits for /api/repo
GITHUB_TOKEN=

# Optional: if you wire /api/contact to forward to Formspree
FORMSPREE_ENDPOINT=